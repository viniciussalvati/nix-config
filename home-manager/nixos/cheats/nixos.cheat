% nixos

# Build and switches to new nixos config --- you MUST be in the nix-config directory
sudo nixos-rebuild switch --flake .

# Build new nixos config and enables it to be booted --- you MUST be in the nix-config directory
sudo nixos-rebuild boot --flake .
