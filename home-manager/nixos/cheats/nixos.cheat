% nixos

# Builds and switches to new nixos config
nh os switch --ask

# Updates the flakes, builds and switches to new nixos config
nh os switch --update --ask

# Builds the new nixos config and enables it to be booted
nh os boot --ask

# Updates the flakes, builds the new nixos config and enables it to be booted
nh os boot --update --ask
